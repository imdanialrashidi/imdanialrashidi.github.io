# Implementation Plans

Generated by the improve skill on 2026-08-21 (audit at commit `944f128`) — second pass 2026-08-22 at commit `dbda97e`.
Execute in the order below unless dependencies say otherwise. Each executor:
read the plan fully before starting, honor its STOP conditions, and update
your row when done.

**Repo-specific executor rule:** this repository enforces owner-controlled
Git (`docs/GIT_POLICY.md`, `AGENTS.md`). Executors never branch/commit/push —
leave verified working-tree diffs for the owner.

**Drift caveat:** at audit `944f128` nearly all product files were **untracked**
(`src/`, `package.json`, …) — every plan 001–010's drift check said to compare
excerpts against the working tree directly. Since commit `dbda97e` the product
source is tracked; plans 011+ use `git diff --stat dbda97e..HEAD -- <paths>` as a
real drift check.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | CI installs project dependencies before verification | P1 | S | — | DONE |
| 002 | Deploy built Astro site to GitHub Pages via Actions | P1 | M | 001 | DONE |
| 003 | Repo hygiene — README, gitignore, leftover file, stale docs | P2 | S | — (land before first product commit) | DONE |
| 004 | Mobile nav dialog: real focus trap + header-height token | P2 | S | — | DONE |
| 005 | Social preview image (og:image/twitter:image) | P2 | M | — | DONE |
| 006 | Single-source contact/social data via site.ts | P2 | S | — | DONE |
| 007 | Product-site route in .pi/verification.json | P2 | S | — | DONE |
| 008 | Migrate off deprecated `z` from astro:content | P3 | S | — | DONE |
| 009 | Remove dead reveal-animation scaffolding | P3 | S | — | DONE |
| 010 | Dependency-audit baseline + npm under Dependabot | P3 | S | — (best after 001/002) | DONE |
| 011 | Harden the mobile-nav dialog so focus cannot escape behind aria-modal | P1 | S | — | DONE |
| 012 | Single-source project data — shared card mapping, draft filtering, and first product tests | P1 | M | — (best before any new project) | DONE |
| 013 | Claim three one-line LCP wins — fetchpriority, eager→lazy, inline CSS | P2 | S | — | DONE |
| 014 | Gate the deploy workflow on checks — don't ship broken types | P1 | S | — | DONE |
| 015 | Harden the OG generator — anchor paths, fail loud, wire or verify at build | P2 | S | — | DONE |
| 016 | Refresh stale docs — CHANGELOG, CONTRIBUTING, DESIGN budgets, ARCHITECTURE topology | P2 | S | 011–015 ideally before (so CHANGELOG describes final state) | DONE |
| 017 | Bring product dependencies under the integrity ledger (or document harness-only scope) | P2 | S–M | — | DONE |
| 018 | Add a lint/format baseline (Biome) — stop re-litigating style in agent sessions | P2 | S–M | — (if doing 012 + 018 together, do 018 first so format diff doesn't mix with logic) | DONE |
| 019 | Minor polish — footer year, placeholder a11y, theme-toggle pressed state (+ models.env guard) | P3 | S | — | DONE |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- 002 requires 001: deploy.yml calls `bash scripts/ci-install.sh`; and a
  deploy workflow is only trustworthy once CI can actually build.
- 003 should land before the owner's first product commit so the initial
  commit is clean (no leftover file, `.astro/` already ignored, README true).
- 001 Step 2 (npm cache in setup-node) is intentionally conditional — enable
  it only after `package-lock.json` is committed (done at `dbda97e`).
- 012 creates `src/lib/projects.ts` — the first product test seam. Any new
  project/card work or future case-study deepening should come after 012 so it
  uses the single source instead of reintroducing drift.
- 014 adds `npm run check` before `npm run build` in `deploy.yml`. It is the
  deploy-side counterpart to `quality.yml`'s `bash scripts/verify.sh` gate.
- 015 wires `scripts/generate-og.mjs` into the build. After it lands, branding
  copy changes automatically refresh `public/og-default.png`; before it, the
  image goes stale silently.
- 016 ideally lands after 011–015 so its CHANGELOG entry can describe the
  batch. It also fixes the `CONTRIBUTING → README` dead pointer and the
  `DESIGN.md`/`ARCHITECTURE.md` staleness that would otherwise mislead a
  reviewer mid-batch.
- 017 extends `.pi/package-integrity.json` to product deps (or documents
  harness-only). It can land any time, but `quality.yml`'s
  `verify-package-integrity --online` will then cover product deps too.
- 018 (Biome) is best before 012 if both are pending — the initial format diff
  touches `src/` broadly; doing it first keeps plan 012's logic diff clean.
  If 018 lands later, the owner should expect a formatter-only follow-up diff
  that touches many files again.
- 019 bundles three isolated polishes (footer, placeholders, theme toggle) plus
  a one-line `.pi/models.env` guard comment. It can land last; its surfaces
  don't interact with 011–018.

## Backlog (vetted findings without plans — ask to expand any into a plan)

- **Direction — case-study long-form content**: collections + case-study
  layouts exist; both project bodies are honest stubs. Authoring pass is
  gated on real screenshots/copy per the no-invented-proof rule (owner's own
  plan, Slice 4 remainder).
- **Direction — post-deploy live verification** (now unblocked — deploy.yml
  live): `curl -I https://imdanialrashidi.github.io/` → 200 custom HTML not
  Jekyll fallback, internal href crawl, sitemap/robots fetch, OG image fetch,
  404 status. Cheapest high-assurance item once Pages is flipped to Actions.
- **Direction — Lighthouse budgets → CI** (design promise, deferred):
  `docs/DESIGN.md` targets LCP ≤2.5s / CLS ≤0.1 / INP ≤200ms, lab via Lighthouse
  preview manual today; exec-plan §6/§8 promised `lighthouseci` assertions
  (`categories:performance ≥95`, `LCP ≤2500`, `CLS ≤0.1`). Deferred — tradeoff
  is CI minutes + flake budget.
- **Deferred docs reconciliation** — `docs/PRODUCT.md` + `docs/PLAN.md`
  remain empty templates intentionally (plans/003 records owner choice vs the
  active plan §4 `Do not leave them templated`). Owner arbitration needed before
  any plan fills them.

## Findings considered and rejected

- **mailto exposure / spam scraping**: documented tradeoff in the active
  plan's risk table (§6); obfuscation explicitly optional. Not a finding.
- **`p` launcher filename**: intentional canonical entrypoint (CHANGELOG
  documents `./p`; CONTRIBUTING references it). Do not rename.
- **Theme system honoring localStorage + prefers-color-scheme**: by design,
  ADR'd in ARCHITECTURE.md; FOUC guard verified.
- **Static/no-framework/no-analytics stack**: explicit ADR decisions
  (rejected-complexity list). Settled.
- **JSON-LD injected via `set:html`**: values are build-time constants from
  `site.ts`; no user input reaches the sink. No injection surface.
- **Canonical trailing-slash normalization in Layout.astro**: correct for
  `trailingSlash: "never"` + sitemap behavior. Leave alone.
- **Hero entrance animation starting at opacity:0**: reduced-motion fallback
  present; animations are compositor-only with forwards fill. Fine.
- **`ProjectMedia` initials derivation edge case** (double-space titles):
  titles are author-controlled collection data; not worth defending.
- **`.pi/models.env` committed to PUBLIC repo** — flagged by auditors as
  potential credential exposure. Vetted: file is committed since `a55dbf5`
  (tracked, required by `scripts/pi-doctor.sh:required`), but on inspection
  (names only) contains 3 non-secret Pi flags (`PI_TELEMETRY`,
  `PI_SKIP_VERSION_CHECK`, `PI_CACHE_RETENTION`). No rotation needed. Downgraded
  to hygiene: plan 019 adds a `COMMITTED — do not add keys` guard comment. A
  future `PI_API_KEY` style value must never be committed (public repo) — if a
  secret was ever pushed, rotate immediately.
- **"Residual zod deprecation hints contradict plan 008 DONE"**: vetted —
  `npm run check` at `dbda97e` = 0 errors / 0 warnings / 0 hints. The
  `astro/zod` import is the post-migration state; exec-plan quotes about 31
  hints were pre-migration and are now stale. Closed.
- **`npm audit` UNPROVEN**: vetted — `npm audit
  --registry=https://registry.npmjs.org` at `dbda97e` = 0 vulnerabilities
  (mirror registry `npmmirror` lacks the audit endpoint). Only `typescript`
  patch `5.9.2→5.9.3` is available (`7.0.2` major exists but risky with
  `@astrojs/check` — not a finding worth planning).
- **X handle spelling `imdaniarshidi` (missing "l")**: already recorded as
  open question Q3 in `docs/exec-plans/active/premium-personal-brand.md`
  (`Use exact strings from brief, with … where appropriate`). All handles are
  internally consistent via `site.ts`; owner confirmation still pending. Flagged
  once, not a fix-until-verified.
- **Font program (subset, preload mono, metric overrides, unicode-range)**:
  real but measured payoff is ~40–60% of 139K fonts after tooling cost, and
  `docs/DESIGN.md` already reserves the thesis (Geist vendored, preload Sans
  only). Not worth doing until a Lighthouse trace shows LCP/CLS pressure.
  Deferred as direction, not a defect plan.
- **CSS token hygiene (unused `--text-2xl/3xl…`, off-token `font-weight: 650` etc.), CSS monoliths / eyebrow duplication, `backdrop-filter` GPU cost, CSP/referrer meta, `theme-color` live sync**: all real observations with near-zero or needs-measurement payoff for an 8-page static site. Recorded as
  considered, low priority — not worth a dedicated plan unless a Lighthouse or
  interaction trace justifies them.
