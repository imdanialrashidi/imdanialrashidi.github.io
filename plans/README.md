# Implementation Plans

Generated by the improve skill on 2026-08-21 (audit at commit `944f128`).
Execute in the order below unless dependencies say otherwise. Each executor:
read the plan fully before starting, honor its STOP conditions, and update
your row when done.

**Repo-specific executor rule:** this repository enforces owner-controlled
Git (`docs/GIT_POLICY.md`, `AGENTS.md`). Executors never branch/commit/push —
leave verified working-tree diffs for the owner.

**Drift caveat:** at audit time nearly all product files were **untracked**
(`src/`, `package.json`, `public/`, `astro.config.mjs`, `tsconfig.json`,
`Assets/`). `git diff` cannot detect drift on them; every plan's drift check
says to compare excerpts against the working tree directly.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | CI installs project dependencies before verification | P1 | S | — | DONE |
| 002 | Deploy built Astro site to GitHub Pages via Actions | P1 | M | 001 | DONE |
| 003 | Repo hygiene — README, gitignore, leftover file, stale docs | P2 | S | — (land before first product commit) | DONE |
| 004 | Mobile nav dialog: real focus trap + header-height token | P2 | S | — | DONE |
| 005 | Social preview image (og:image/twitter:image) | P2 | M | — | DONE |
| 006 | Single-source contact/social data via site.ts | P2 | S | — | DONE |
| 007 | Product-site route in .pi/verification.json | P2 | S | — | DONE |
| 008 | Migrate off deprecated `z` from astro:content | P3 | S | — | DONE |
| 009 | Remove dead reveal-animation scaffolding | P3 | S | — | DONE |
| 010 | Dependency-audit baseline + npm under Dependabot | P3 | S | — (best after 001/002) | DONE |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- 002 requires 001: deploy.yml calls `bash scripts/ci-install.sh`; and a
  deploy workflow is only trustworthy once CI can actually build.
- 003 should land before the owner's first product commit so the initial
  commit is clean (no leftover file, `.astro/` already ignored, README true).
- 001 Step 2 (npm cache in setup-node) is intentionally conditional — enable
  it only after `package-lock.json` is committed.

## Backlog (vetted findings without plans yet — ask to expand any into a plan)

- **Direction — case-study long-form content**: collections + case-study
  layouts exist; both project bodies are honest stubs. Authoring pass is
  gated on real screenshots/copy per the no-invented-proof rule (owner's own
  plan, Slice 4 remainder).
- **Post-deploy live verification**: once plans/002 lands and the owner flips
  Pages to Actions, run a social-card debugger check for plan 005's image and
  a link crawl (`every internal href resolves`) from Slice 3 of the active
  plan.

(Former backlog items — contact single-sourcing, verification route,
deprecated `z`, dead reveal code, dependency audit — are now plans 006–010.)

## Findings considered and rejected

- **mailto exposure / spam scraping**: documented tradeoff in the active
  plan's risk table (§6); obfuscation explicitly optional. Not a finding.
- **`p` launcher filename**: intentional canonical entrypoint (CHANGELOG
  documents `./p`; CONTRIBUTING references it). Do not rename.
- **Theme system honoring localStorage + prefers-color-scheme**: by design,
  ADR'd in ARCHITECTURE.md; FOUC guard verified.
- **Static/no-framework/no-analytics stack**: explicit ADR decisions
  (rejected-complexity list). Settled.
- **JSON-LD injected via `set:html`**: values are build-time constants from
  `site.ts`; no user input reaches the sink. No injection surface.
- **Canonical trailing-slash normalization in Layout.astro**: correct for
  `trailingSlash: "never"` + sitemap behavior. Leave alone.
- **Hero entrance animation starting at opacity:0**: reduced-motion fallback
  present; animations are compositor-only with forwards fill. Fine.
- **`ProjectMedia` initials derivation edge case** (double-space titles):
  titles are author-controlled collection data; not worth defending.
